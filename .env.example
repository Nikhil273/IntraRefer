# Root environment variables (if needed)
NODE_ENV=development

# Frontend environment variables (create client/.env)
REACT_APP_API_URL=http://localhost:5000/api
REACT_APP_RAZORPAY_KEY_ID=

# Backend environment variables (create server/.env)
# See server/.env.example for complete backend configuration